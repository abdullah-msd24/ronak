{
  "name": "ronak",
  "version": "1.0.0",
  "private": true,
  "build": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^1.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^10.0.0",
    "prisma": "^5.22.0"
  }
}
